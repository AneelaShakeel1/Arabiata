import {StyleSheet} from 'react-native';
import {wp} from '~/epcore/utils';

const styles = StyleSheet.create({
  header: {
    fontSize: 14,
  },
  expandView: {
    flex: 1,
    paddingHorizontal: 13,
  },
});

export default styles;
